{
  "name": "mintvale-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "A static MMO-inspired portfolio with file-based content",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": { "node": ">=20" }
}
